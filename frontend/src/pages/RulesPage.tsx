import PlaceholderPage from './PlaceholderPage';

export default function RulesPage() {
  return (
    <PlaceholderPage
      title="CPA rule editor"
      body="Create, overlap-check, and preview rules here next."
    />
  );
}
